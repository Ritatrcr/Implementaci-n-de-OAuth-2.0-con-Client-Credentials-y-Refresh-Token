# === OIDC ===
AUTH_ISSUER=http://localhost:8080/realms/OAuthRealm

# Si no defines JWKS_URI, se deriva automáticamente del issuer (Keycloak):
# JWKS_URI=https://auth.example.com/realms/rm-patrones/protocol/openid-connect/certs

TOKEN_LEEWAY_SECONDS=60               

# === Seguridad transporte ===
HTTPS_ONLY=true

# === CORS ===
ALLOWED_ORIGINS=https://frontend.example.com,https://web.postman.co

# === Servidor ===
PORT=8443
TLS_CERT_PATH=./certs/server.crt
TLS_KEY_PATH=./certs/server.key
